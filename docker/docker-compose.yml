version: '3.8'

services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: gunicorn CourseCompass.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ../:/app
    ports:
      - "80:8000"
    env_file:
      - .env

